# v0.4.0 下一步工作计划

> **当前状态**: 核心功能已完成，但生产接线未完成  
> **目标**: 完成集成并发布 v0.4.0

---

## 📊 现状评估

| 模块 | 状态 | 说明 |
|------|------|------|
| M1: 多 Agent 协作 | ✅ 代码完成 | `internal/agentipc/collaboration.go` |
| M2: Evolution-Runtime 集成 | ✅ 代码完成 | `internal/aresrecovery/evolution_*.go` |
| M3: 可解释性 | ⚠️ 部分完成 | 核心层完成，Dashboard 未连接 |
| M4: 全局观测 | ⚠️ 部分完成 | 核心层完成，API 未暴露 |
| 生产接线 | ❌ 未完成 | `cmd/ares/serve.go` 未调用新组件 |

---

## 🔧 下一步具体工作

### Step 1: 生产接线（P0 - 必须完成）

**文件**: `cmd/ares/serve.go`

```go
// 需要添加的连接代码

// 1. 创建 EvolutionAwareSpawner
spawnerSource := &EvolutionSpawnPolicyProvider{...}
spawner := aresrecovery.NewEvolutionAwareSpawner(kernel.agents, spawnerSource)

// 2. 创建 EvolutionAwareQuotaManager
quotaSource := &EvolutionQuotaPolicyProvider{...}
quotaManager := aresrecovery.NewEvolutionAwareQuotaManager(kernel.agents, quotaSource)

// 3. 创建 EvolutionAwareIPC
ipcSource := &EvolutionIPCProtocolProvider{...}
ipc := aresrecovery.NewEvolutionAwareIPC(bus, ipcSource)

// 4. 创建 GlobalTracer
tracer := aresrecovery.NewGlobalTracer()

// 5. 创建 FeedbackStore
feedbackStore := aresrecovery.NewFeedbackStore()

// 6. 创建 EvolutionTracer
evolutionTracer := aresrecovery.NewEvolutionTracer()
```

---

### Step 2: Dashboard API 实现（P1 - 推荐完成）

**文件**: `internal/dashboard/api_handlers.go`

```go
// 需要添加的 API Handler

// GET /api/v2/evolution/traces
func (a *APIv2) getEvolutionTraces(c *gin.Context) {
    // 返回 EvolutionTracer.Snapshot()
}

// POST /api/v2/evolution/feedback
func (a *APIv2) postEvolutionFeedback(c *gin.Context) {
    // 接收 HumanFeedback 并写入 FeedbackStore
}

// GET /api/v2/observability/spans
func (a *APIv2) getObservabilitySpans(c *gin.Context) {
    // 返回 GlobalTracer.Spans()
}

// GET /api/v2/observability/traces/:id
func (a *APIv2) getObservabilityTrace(c *gin.Context) {
    // 返回单个 TraceSpan
}
```

---

### Step 3: 补充测试（P1 - 推荐完成）

**文件**: `cmd/ares/kernel_test.go` 新增测试

```go
// 测试 EvolutionAwareSpawner 与 Kernel 的集成
func TestKernelWithEvolutionSpawner(t *testing.T) {}

// 测试 EvolutionAwareQuotaManager 与 Kernel 的集成
func TestKernelWithEvolutionQuota(t *testing.T) {}

// 测试 Feedback API 端点
func TestPostEvolutionFeedback(t *testing.T) {}

// 测试 Traces API 端点
func TestGetEvolutionTraces(t *testing.T) {}
```

---

### Step 4: 添加示例应用（P2 - 可选）

**目录**: `examples/23-evolution-integration/`

```go
// 演示 Evolution-Runtime 集成的完整流程
package main

func main() {
    // 1. 创建 Kernel
    // 2. 配置 Evolution 策略源
    // 3. 启动多 Agent 协作
    // 4. 查看进化轨迹
}
```

---

### Step 5: 文档更新（P2 - 可选）

**文件**: 
- `README.md` - 添加 v0.4.0 特性列表
- `docs/articles/zh/25-v0.4.0-release-notes.md` - 发布说明
- `docs/articles/en/25-v0.4.0-release-notes.md` - 英文版

---

## 📅 建议时间安排

```
Day 1-2: Step 1 生产接线
  ├─ 连接 EvolutionAwareSpawner
  ├─ 连接 EvolutionAwareQuotaManager
  └─ 连接 EvolutionAwareIPC

Day 3-4: Step 2 Dashboard API
  ├─ 实现 getEvolutionTraces
  ├─ 实现 postEvolutionFeedback
  └─ 实现 getObservabilitySpans

Day 5-6: Step 3 测试
  ├─ 单元测试
  ├─ 集成测试
  └─ race detector 验证

Day 7-8: Step 4-5 示例与文档
  ├─ 添加 examples/23-evolution-integration/
  └─ 更新 README 和 release notes

Day 9-10: 最终验收
  ├─ go test ./...
  ├─ go vet ./...
  └─ 人工 review
```

---

## ✅ 验收标准

完成以下步骤后，可以发布 v0.4.0：

- [ ] `go test ./...` 全部通过
- [ ] `go test -race ./...` 无数据竞争
- [ ] `go vet ./...` 零警告
- [ ] Kernel 能成功创建 EvolutionAwareSpawner/QuotaManager/IPC
- [ ] Dashboard API 能返回进化轨迹数据
- [ ] 能提交人工反馈并影响适应度计算
- [ ] 示例应用能运行

---

## 🎯 最低可行发布（MVP）

如果时间有限，至少完成：

1. **Step 1 生产接线**（核心功能才能用）
2. **Step 3 测试**（保证质量）

其他步骤可以延后到 v0.4.1。
