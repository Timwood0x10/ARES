# Autonomous Evolution Demo Configuration
#
# NOTE: This demo now uses the PROJECT-LEVEL config for evolution settings.
# To enable GA scenarios (6 & 7), add to your project's config.yaml:
#
#   evolution:
#     enabled: true
#     population_size: 20
#     generations: 15
#
# Or use environment variable: EVOLUTION_ENABLED=true
#
# Scenarios 1-5 (core building blocks) always run and require no configuration.
# This file is kept for reference only — it is NOT loaded at runtime.
