# ARES 深度 Review：收敛路径与闭环缺口

> 审查对象：`~/go/src/goagent`（dev 分支，HEAD `c5f36d45`）+ 7 项未提交改动
> 审查时间：2026-09-02
> 审查方式：静态分析 + 实测编译/vet/测试，**未做任何提交，未修改任何工作区文件**
> 实测结果：`go build ./...` 零错误 · `go vet ./...` 零警告 · `internal/ares_evolution` 测试全绿

---

## 0. 执行摘要

当前 dev 分支正在完成一件明确的事：**让"零 token 模式下的进化闭环"真正闭合**——不是让代码跑起来，而是让 G2 shadow gate 声称的"独立证据"名副其实。

已完成 C2.1 → C2.2 → C2.6 → C3.2 四级链路，方向和质量都对。测试覆盖了修复的核心主张（`TestShadowSamplerUsesDisjointReplayWindows`、`TestShadowSamplerReplayProducesRealWinRate`）。

但存在三个判断：

1. **闭环尚未端到端验证。** 单元测试证明了每个零件正确，没有测试证明"零 LLM 配置下一个候选策略能真的走完 G1→G2→G3→G4 并被晋升"。G2 现在是 fail-closed，这个状态下系统可能表现为"永不晋升"——正确但无用。
2. **架构收敛尚未开始，且是当前最大的复利负债。** 双进化系统（v1 被 51 个文件引用 / v2 被 32 个）+ cmd 层 18,015 行编排逻辑，两者叠加会让下一次"稳定升级"的成本显著高于这一次。
3. **代码里的诚实标注是资产。** 46 处自述未闭合、10 处 `TODO(tech-debt)`、GAP 编号登记——这在同类规模项目里罕见，请保持。

---

## 1. 项目体检数据

| 指标 | 数值 | 判读 |
|---|---|---|
| Go 总行数 | 360,605 | 大型单体 |
| 非测试代码 | 181,446 | — |
| 测试代码 | 179,159（**49.7%**） | 极高；609 个测试文件 |
| internal 包数 | 49 | 超出单人可导航范围 |
| cmd/ | 18,015 行 | **异常**：CLI 层承载内核编排 |
| internal/ | 310,500 行 | 其中 `ares_evolution` 56,708 行 |
| api / sdk | 7,137 / 10,023 | SDK 存在但拿不到编排逻辑 |

测试占比接近 50%，在 16 star 的项目里属于极端值。它保证了正确性，但也意味着**每加一个功能要同时维护等量测试**——这会放大架构债务的成本。

---

## 2. 本次未提交工作的评价

6 个文件构成一条完整证据链：

```
C2.1  kernelscheduler/scheduler.go      捕获真实 latency / retries（修复死权重）
C2.6  ares_evolution/observer.go        Source 提取为常量 observerEvidenceSource
C3.2  ares_evolution/replay_scorer.go   ReplayScorer：按策略自身历史评分，零 LLM
C3.2  ares_evolution/shadow_sampler.go  每次比较分配不相交时间窗口
C2.6  ares_evolution/genome_wiring_system.go  改为运行时注入评分器
C3.2  cmd/ares/peer_mode.go             注入 ReplayScorer，无 store 则保持 fail-closed
```

### 2.1 三处判断准确的地方

**a) 识别出"统计显著性伪装"**

`shadow_sampler.go` 里被替换掉的旧注释承认：确定性评分器下 N 次采样产生 N 个**完全相同**的比较，MinSamples 被重复而非证据满足——原文 *"a single score comparison wearing MinSamples' clothes"*。这是本次 review 中最有价值的一句自我诊断。解法（每次比较读取 `[now-(i+1)·span, now-i·span)` 不相交窗口）把胜率从坍缩的 0.0/1.0 变成真实分布。

**b) 识别出"缺陷转移"而非"缺陷修复"**

`peer_mode.go` 注释：只注入一个全局归因分数，候选和线上策略拿到同一个数，`ShadowWon` 要求 `shadow > active`，于是每次比较都是平局，胜率 0.0，G2 仍然拒绝一切——**缺陷只是换了个位置**。这个判断直接导向了"必须按策略区分"的设计。

**c) 拒绝用假证据换绿灯**

无 evidence store 时不注入评分器，让 G2 保持 fail-closed，而不是让它基于平局做出判断。这与 `lifecycle.go:520` 的 fail-closed 分支一致。

### 2.2 一处建议：`replayWindowSpan` 硬编码

`replayWindowSpan = 10 * time.Minute` 的取值依据写在注释里（观察器写入速率 vs MinSamples 覆盖视界），推导合理。但它是包级常量，无法按部署调整。

建议：挪进 `SystemConfig`，默认保持 10 分钟。理由不是灵活性，而是**这个参数直接决定证据的独立性**，运维需要能看见它。同理 `replayQueryLimit = 200`。

---

## 3. 闭环缺口清单（按严重度）

### P0-1 端到端闭环未验证 —— 缺口最要紧

**现象**：G2 现在 fail-closed，ReplayScorer 已注入。但没有任何测试证明：在零 LLM 默认配置下，一个候选策略能走完 `Submit → G1 → G2 → G3 → G4 → Promote` 并进入 ACTIVE。

**风险**：当前状态很可能是"永不晋升"——逻辑正确、系统无用。而单元测试全绿会让这一点不可见。

**验证方法**（建议直接写成集成测试）：

```go
// 零 LLM 配置，注入内存 evidence store
// 1. 预置若干条 KindFitness 记录（source="strategy"），覆盖多个 10 分钟窗口
// 2. 提交一个候选策略
// 3. 断言：最终状态 == StateActive，且 shadow 报告 TotalComparisons >= MinSamples
// 4. 反向用例：空 evidence store → 断言状态停留在 StateShadow（fail-closed 生效）
```

没有这个测试，"闭环"目前只是**各零件正确**，不等于**链条导通**。

### P0-2 `ConstantScorer(50.0)` 兜底仍在

- `internal/ares_evolution/genome_wiring_system.go:289`
- `internal/ares_evolution/genome_wiring_run.go:399`（注释标明"为向后兼容保留"）

常量评分器对任何策略返回同一个数，是 tie deadlock 的源头。当前路径已被 ReplayScorer 覆盖，但兜底仍在：若 `cfg.Scorer == nil` 且 replay 未注入，会退化到常量。

建议：零 token 模式下**显式拒绝**而非静默兜底——返回 error 或让 gate fail-closed，与你在 G2 上采取的策略保持一致。向后兼容的代价在这里是"静默无信息"。

### P1-1 Per-task A/B 执行路径缺失（你已自行标注）

`shadow_sampler.go` 注释：*"Per-task A/B execution (running the candidate on live traffic) is what would make it candidate-specific; that path does not exist yet."*

这是 ReplayScorer 的根本限制：候选从未执行过，只能回退到 prior，其判定实际是"当前机队质量 vs 线上策略历史"，**不是候选特异性的**。

这是设计上诚实的选择，但要知道：在补齐这条路径前，G2 的 verdict 语义是"线上策略是否拖累质量"，而非"候选是否更好"。README/设计文档若不写明这点，外部读者会误判。

建议路径：候选在真实任务上影子执行（结果只记录、不影响线上响应），这是唯一能让候选产生自身证据的方式。工作量不小，但这是让进化闭环从"能跑"变"有意义"的分水岭。

### P1-2 G3 / G4 在零 token 模式下的可用性待审计

门控链路 `G1 Guardrail → G2 Shadow → G3 Eval Suite → G4 Deployment staging`。本次工作集中在 G2。需要确认：

- **G3** 依赖 `ares_eval.EvaluatorRegistry`——零 LLM 配置下 registry 是否为空？为空时 G3 是 pass 还是 fail？若无条件 pass，则 G3 是橡皮图章。
- **G4** 依赖 deployment staging——默认配置下是否已接线？

建议：按 G2 的标准审计一遍，把结果写进 CHANGELOG（像 fencing epoch 那样）。

### P1-3 `lifecycle.go:13-14` 注释与实现可能不一致

文件头注释：*"The lifecycle is nil-safe: when not wired (legacy mode), the adapter falls back to the old unconditional deploy path"*。

但 `Submit`（:588）已替代旧的无条件部署（B2 fix），且 `shadowVerifyGate` 是 fail-closed（:520）。若为 adapter 层的 legacy 兼容路径，建议明确写出触发条件；若已废弃，删掉注释。这类注释会让后续维护者误以为存在绕过门控的路径。

---

## 4. 架构收敛路径

### 4.1 双进化系统 —— 最优先

| | v1 `ares_evolution` | v2 `evolution` |
|---|---|---|
| 规模 | 56,708 行 | 9,011 行 |
| 外部引用文件数 | 51 | 32 |
| 进化对象 | 策略 / 记忆 | 工作流 DAG |
| 基因组 | `genome/` | `genome/`（`NewWorkflowGenome`） |
| G3 门控 | `gate_eval.go` | `gate3_orchestrator.go`（159 行） |

**两套都在活跃使用，都不能删。** 这是收敛的难点，也是必须做的原因：两套 `genome`、两套 G3、两套 scoring 概念，认知成本约等于维护两个子系统。

建议分两步（不要一次性重写）：

1. **先抽共享层**：把 G1–G4 验证流水线、evidence 读写、promotion 抽到 `internal/evolution/shared/`（或 `verify/`），两套系统复用同一套门控语义。这一步不改变外部行为，可独立验证。
2. **再统一命名空间**：`evolution/strategy`（原 v1）与 `evolution/workflow`（原 v2）归于同一父包，消除 `ares_evolution` / `evolution` 的命名歧义。

第 1 步的收益最大：门控语义统一后，"G2 fail-closed"这类正确性能自动覆盖两套系统，而不用维护两份。

### 4.2 cmd 层下沉

`cmd/ares/` 18,015 行，`package main` 里跑着三个内核循环（`kernel_loop.go`）：recovery sweep、quota apply、evolution apply——而 `internal/` 下已有 `ares_runtime`、`aresrecovery`、`kernelscheduler` 对应包。

后果不是"不能测"（你已给 cmd 写了测试，13.6 秒通过），而是**SDK 用户拿不到编排逻辑**。`sdk/` 有 10,023 行，但真正的运行时编排锁在 main 包里无法 import，这与"一行代码启动 Agent"的定位冲突。

建议：三个循环下沉到 `internal/system_runtime`，cmd 只留 CLI 解析与启动装配。可增量做，一次迁一个循环。

### 4.3 包命名重叠

49 个 internal 包中存在多组概念重叠：`ares_runtime` ↔ `system_runtime`、`llm` ↔ `llmservice`、`eval` ↔ `ares_eval`。

建议：在 4.1 的命名空间统一时一并梳理，单独做收益不大。

---

## 5. 下一步行动建议（按周）

| 优先级 | 动作 | 产出 | 判据 |
|---|---|---|---|
| **W1** | 端到端闭环集成测试（P0-1） | 1 个 integration test | 零 LLM 下候选能晋升；空 store 下停滞 |
| **W1** | 清理 `ConstantScorer` 兜底（P0-2） | 2 处改动 | 零 token 模式显式拒绝而非静默退化 |
| **W2** | 审计 G3/G4 零 token 可用性（P1-2） | CHANGELOG 条目 | 明确每级门控在默认配置下的真实行为 |
| **W2** | `replayWindowSpan` / `replayQueryLimit` 配置化 | SystemConfig 字段 | 参数可见可调 |
| **W3–W4** | 抽共享验证流水线（4.1 步骤 1） | `evolution/verify/` | 两套系统复用同一门控语义 |
| **W5+** | cmd 三循环下沉（4.2） | `internal/system_runtime` | SDK 可复用编排 |
| 持续 | per-task A/B 执行路径设计（P1-1） | 设计文档 | 候选产生自身证据 |

**顺序理由**：W1 的两项决定"闭环是否真的闭合"，且工作量小。在此之前投入架构重构，等于在不确定地基上施工。4.1 的收益最大但成本也最高，应在闭环验证通过后再启动。

---

## 6. 工程卫生（低优先级，顺手可做）

- 根目录 `cover.out`（覆盖率产物）与 `a.excalidraw` 建议加入 `.gitignore`
- 根目录 `ares-evolution-loop-closure-plan-zh.md`（883 行）、`ares-evolution-loop-tasklist-zh.md`（182 行）建议移入 `docs/`
- `benchmarks/` 与 `test/` 目录当前无 `.go` 文件，确认是否仍需要

---

## 7. 值得肯定的部分

这几条不是客套，是 review 中确实验证过的：

1. **fencing epoch 的修复**（CHANGELOG 0.3.1）：识别出 epoch 扫描范围收窄导致崩溃前令牌被重新发放，让失效持有者通过所有权检查。这是分布式所有权的经典陷阱，且写进了 CHANGELOG 而非静默修复。
2. **"缺陷转移"意识**：`peer_mode.go` 注释明确论证了"只修症状会让 defect 换个位置出现"，这在 AI 辅助开发中尤其稀缺。
3. **失败模式优先于成功路径**：`replay_scorer.go` 的冷启动设计不选择"编造一个有利数值"，而选择回退到可辩护的 prior，并把语义写清楚——"候选仅在机队当前表现优于线上策略历史时被晋升"。
4. **技术债显式登记**：46 处自述未闭合 + 10 处 `TODO(tech-debt)` + GAP 编号。隐藏的债比登记的债危险得多。

---

## 附录：本次审查的验证记录

```
go build ./...                        exit 0（零错误）
go vet ./...                          exit 0（零警告）
go test ./internal/ares_evolution/ -run "Replay|Shadow" -v    全 PASS
go test ./internal/ares_evolution/... ./internal/evolution/... ./cmd/...   17 包全 ok
```

未提交改动未做任何修改，未执行任何 git 写操作。
